import React from "react";


function Home_(props)
{
    return (
        <div>
            <center><h1>Home</h1></center>
        </div>
    );
}
  
export default Home_;

/*
isAuthenticated && (
    <div>
        <img src={user.picture} alt={user.name} />
        <h2>{user.name}</h2>
        <p>{user.email}</p>
        <h5>{ userCookies.token }</h5>
        </div>
)
*/